mode: parallel #模式 balance(均衡模式)、parallel(平行模式)
containers:
  - address: http://0.0.0.0:0000 #面板访问地址
    username: admin
    password: admin
    cid: admin #青龙系统设置
    secret: admin #青龙系统设置
    weigth:  
    mode: parallel
    limit: 9999
AtTime:  0,12 #转换wskey时间。
Wskey: true # 填空默认禁用wskey转换 需要的填true
IsOldV4: true #填写true或者false  false是否A1或者旧版V4
theme:   #自定义主题，支持本地、网络路径
static: ./static
master:  #管理员账户
database:  #数据库位置
qywx_key: #企业微信机器人key
daily_push:
resident:
user_agent:
telegram_bot_token:
telegram_user_id:
qquid:  #接收通知的qq号
qqgid:  #监听的群
qbot_public_mode: false
default_priority:
no_ghproxy: true
daily_asset_push_cron: 0 9 * * * #日常资产推送时间
repos:
  - git: 